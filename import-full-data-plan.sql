-- ============================================================
-- 全データインポート計画
-- ============================================================
-- 現在の状態: 
--   - 管理者: 3名（既存）
--   - ユーザー: 6名（テストデータ）
--   - 購入: 3件（テストデータ）
--
-- 必要な作業:
--   1. テストユーザー削除（6名）
--   2. 本番ユーザー追加（521名）
--   3. 本番購入データ追加（430件）
--   4. その他のテーブル追加
-- ============================================================

-- Step 1: テストデータを削除（管理者は残す）
DELETE FROM purchases;
DELETE FROM affiliate_cycle;
DELETE FROM users;

-- Step 2以降: 本番データをインポート
-- （次のファイルで実行）
